

````markdown
# 半自动合约交易 Agent 系统 — 架构计划书

#trading #architecture #planning #multi-agent

> [!NOTE] 版本说明
> v1.1 目标：**只分析，不下单**。系统负责采集数据、生成信号、风险审查、推送 TG；你负责最终交易决策。
>
> 你只通过 **Telegram** 和系统对话，不需要人工分别操作 Codex、Claude、Hermes、OpenClaw。

---

## 一、系统目标

- 24 小时监控主流高流动合约，默认 BTC / ETH / SOL，后续支持白名单扩展。
- 默认分析周期：15m / 1h / 4h。
- 每 15 分钟生成一次市场快照，满足条件时主动推送交易信号。
- 输出趋势、入场区、止损、分批止盈、卖出/减仓提醒、信号失效条件。
- 通过 Telegram 推送信号卡片，并支持命令式查询。
- 所有行情、信号、风险审查、人工交易结果写入本地数据库和 Obsidian，支持复盘。

---

## 二、核心原则

> [!WARNING] 第一版边界
> - 不自动下单。
> - OKX API 第一版只开只读权限。
> - 外部事件不能单独触发交易，只能作为事件因子进入审查。
> - 没有明确止损、止盈、失效条件的信号不推送为交易建议。
> - Claude 风险审查可以把任何信号降级为“观望”。

---

## 三、整体架构

```mermaid
flowchart TD
    U["你"] <--> TG["Telegram Bot<br/>唯一交互入口"]
    TG <--> OC["OpenClaw<br/>总调度 / 对话路由 / 告警推送"]

    OC --> SCH["定时任务<br/>15m / 1h / 4h"]
    SCH --> DATA["数据采集服务"]

    DATA --> OKX["OKX API<br/>K线 / ticker / 资金费率 / 持仓量 / 标记价格"]
    DATA --> CG["CoinGecko API<br/>全市场 / 热门币 / 板块 / 衍生品"]

    OC --> H["Hermes<br/>外部信息采集"]
    H --> X["X / Twitter<br/>xurl / xitter / opencli"]
    H --> NEWS["新闻 / 公告 / RSS"]

    OKX --> DB["本地数据库<br/>SQLite / DuckDB"]
    CG --> DB
    X --> DB
    NEWS --> DB

    DB --> C["Codex GPT-5.5<br/>指标计算 / 策略引擎 / 初步信号"]
    C --> S["初步交易信号<br/>方向 / 入场 / 止损 / 止盈 / 失效条件"]
    S --> CL["Claude Opus 4.7<br/>风险审查 / 反方推理 / 降级判断"]

    CL --> R["最终信号卡片"]
    R --> OC
    OC --> TG
    OC --> OB["Obsidian<br/>交易日志 / 策略复盘 / 人工确认知识库"]
````

## 四、多 Agent 职责

|Agent|身份|是否直接和你对话|推荐模型|职责|
|---|---|---|---|---|
|OpenClaw|总调度员 / TG 前台|是|按任务调用|Telegram 接入、定时任务、消息路由、告警推送、调用其他 Agent|
|Codex|策略工程师|否|GPT-5.5|数据管线、指标计算、策略规则、初步信号、回测、工程实现|
|Hermes|情报员|否|DeepSeek v4p / Opus 4.7 / 低成本模型|X、新闻、公告、CoinGecko 辅助信息采集，事件摘要和可信度评分|
|Claude|风控审查员|否|Opus 4.7|反方推理、风险审查、是否追高/追空、是否降级为观望|
|Obsidian|复盘记忆库|否|无|保存市场日志、信号、人工交易结果、复盘结论|

### 协作规则

1. 你只和 Telegram 对话。
    
2. OpenClaw 收到 TG 消息或定时任务后，决定调用哪个 Agent。
    
3. Codex 只根据结构化行情和指标生成初步信号。
    
4. Hermes 只负责外部信息采集和事件提炼，不直接给交易方向。
    
5. Claude 负责质疑信号，发现追高、噪声、盈亏比不足时降级。
    
6. OpenClaw 汇总最终结论并推送 TG，同时写入 Obsidian。
    

## 五、交易判断因子

|因子|判断内容|数据来源|负责 Agent|
|---|---|---|---|
|趋势结构|15m / 1h / 4h 是否同向，EMA/MA 多空排列，高低点结构|OKX K线|Codex|
|动量|RSI、MACD、突破、背离|OKX K线|Codex|
|波动率|ATR、布林带宽度、异常波动|OKX K线|Codex|
|成交量|放量突破、缩量反弹、量价背离|OKX 成交量|Codex|
|关键价位|支撑/压力、前高前低、整数位、失效位|OKX K线/盘口可选|Codex|
|合约情绪|资金费率、持仓量、标记价格偏离|OKX 合约数据|Codex|
|市场环境|BTC/ETH 主导趋势、全市场涨跌、热门板块、衍生品热度|CoinGecko|Hermes + Codex|
|外部事件|OKX 公告、项目方公告、新闻、X 项目方/大号动态|X / 新闻 / 公告|Hermes|
|风控审查|是否追高/追空、盈亏比是否足够、是否应该观望|汇总信号|Claude|

### 因子使用原则

- 硬因子优先：价格、成交量、资金费率、持仓量优先于文本消息。
    
- 多周期确认：15m 负责入场节奏，1h 负责日内方向，4h 负责结构背景。
    
- 外部事件只做加权：新闻和 X 不能单独触发交易。
    
- 风控否决优先：Claude 判断风险过高时，信号降级为观望或只发市场观察。
    

## 六、数据源与接入方式

### OKX API

- K线：15m / 1h / 4h。
    
- ticker：最新价格、24h 高低、成交额。
    
- 资金费率：判断多空拥挤程度。
    
- 持仓量：判断趋势是否有合约资金支持。
    
- 标记价格：避免用异常成交价误判。
    

### CoinGecko API

- 全市场市值和成交量。
    
- BTC / ETH 市占率。
    
- 热门币和板块热度。
    
- 衍生品市场排名和资金费率补充。
    

### X / Twitter

- 优先：Hermes 使用 `xurl` 或 `xitter`，走官方 API 路径。
    
- 备选：没有官方 API 时，使用 `opencli twitter` 做只读抓取。
    
- 只采集白名单账号、项目方、交易所公告、关键词搜索结果。
    
- 输出事件摘要、影响币种、来源可信度、传播强度、时间戳。
    

### 新闻 / 公告

- OKX 公告。
    
- 项目方公告。
    
- RSS / 新闻源。
    
- 重大宏观或监管新闻。
    

## 七、Telegram 交互设计

|命令|功能|后台协作|
|---|---|---|
|`/btc`|查看 BTC 当前趋势和信号|OpenClaw → Codex → Claude|
|`/eth`|查看 ETH 当前趋势和信号|OpenClaw → Codex → Claude|
|`/sol`|查看 SOL 当前趋势和信号|OpenClaw → Codex → Claude|
|`/signal`|查看全部最新信号|OpenClaw 读取数据库汇总|
|`/risk BTC`|查看 BTC 风险审查|OpenClaw → Claude|
|`/news BTC`|查看 BTC 相关新闻、X、公告|OpenClaw → Hermes|
|`/watch BTC`|重点监控 BTC，触发止盈/止损/失效条件时提醒|OpenClaw 调度|
|`/journal`|写入当日 Obsidian 复盘|OpenClaw → Obsidian|

### 主动提醒类型

- 新信号提醒。
    
- 止盈触发提醒。
    
- 止损接近提醒。
    
- 卖出/减仓提醒。
    
- 信号失效提醒。
    
- 外部重大事件提醒。
    
- 风险升高提醒。
    

## 八、TG 信号卡片格式

```text
BTC/USDT 合约信号
━━━━━━━━━━━━━━━━━━
方向：做多 / 做空 / 观望
置信度：72%
周期：15m + 1h + 4h 综合

入场区：107,200 - 107,800
止损：105,500（ATR + 结构位）
止盈1：110,000（减仓 30%）
止盈2：113,000（减仓 40%）
止盈3：116,000（趋势延伸，移动止损）

卖出/减仓提醒：
- 价格触及止盈1，提醒减仓
- 1h RSI 顶背离，提醒降低仓位
- 跌破 106,000，信号失效

技术触发：
- 1h EMA 多头排列
- 15m 放量突破前高
- 资金费率温和转正

外部事件：
- 无重大负面事件
- X 讨论热度上升，但未达到事件驱动级别

Claude 风险审查：
- 当前不是最佳追多点，建议只在回踩入场区时考虑
- 若 BTC 跌破 4h 支撑，全部多头信号降级为观望

更新时间：2026-05-26 11:00
```

## 九、目录结构

```text
~/Documents/trading/
├── config.py                 # 本地配置，不进 git
├── main.py                   # 主调度入口
├── data/
│   └── trading.db            # SQLite / DuckDB 本地数据库
├── collectors/
│   ├── okx.py                # OKX 行情和合约数据采集
│   ├── coingecko.py          # CoinGecko 市场数据
│   └── external.py           # 新闻 / 公告 / X 事件入口
├── analysis/
│   ├── indicators.py         # 技术指标计算
│   ├── strategy.py           # 初步信号生成
│   └── risk_review.py        # Claude 风险审查封装
├── agents/
│   ├── codex_runner.py       # Codex GPT-5.5 调用
│   ├── hermes_runner.py      # Hermes 技能调用
│   └── claude_runner.py      # Claude Opus 4.7 调用
├── notify/
│   └── telegram.py           # TG Bot 推送和命令处理
├── scheduler/
│   └── jobs.py               # 定时任务
└── obsidian/
    └── daily_log.py          # 每日复盘写入 Obsidian
```

## 十、数据库设计

```sql
-- 行情快照和技术指标
CREATE TABLE market_data (
    id INTEGER PRIMARY KEY,
    symbol TEXT,
    timeframe TEXT,
    timestamp INTEGER,
    open REAL,
    high REAL,
    low REAL,
    close REAL,
    volume REAL,
    rsi REAL,
    macd REAL,
    ema_fast REAL,
    ema_slow REAL,
    atr REAL,
    bollinger_width REAL
);

-- 合约数据
CREATE TABLE contract_metrics (
    id INTEGER PRIMARY KEY,
    symbol TEXT,
    timestamp INTEGER,
    funding_rate REAL,
    open_interest REAL,
    mark_price REAL,
    long_short_bias TEXT
);

-- 外部事件
CREATE TABLE external_events (
    id INTEGER PRIMARY KEY,
    timestamp INTEGER,
    source TEXT,
    symbol TEXT,
    event_type TEXT,
    title TEXT,
    summary TEXT,
    url TEXT,
    credibility INTEGER,
    impact INTEGER
);

-- 初步和最终信号
CREATE TABLE signals (
    id INTEGER PRIMARY KEY,
    timestamp INTEGER,
    symbol TEXT,
    direction TEXT,
    confidence INTEGER,
    entry_zone TEXT,
    stop_loss REAL,
    take_profit TEXT,
    reduce_position_condition TEXT,
    invalid_condition TEXT,
    reason TEXT,
    status TEXT
);

-- Claude 风险审查
CREATE TABLE risk_reviews (
    id INTEGER PRIMARY KEY,
    signal_id INTEGER,
    timestamp INTEGER,
    reviewer_model TEXT,
    risk_level TEXT,
    verdict TEXT,
    objections TEXT,
    downgrade_reason TEXT
);

-- Telegram 推送记录
CREATE TABLE telegram_messages (
    id INTEGER PRIMARY KEY,
    timestamp INTEGER,
    signal_id INTEGER,
    chat_id TEXT,
    message_type TEXT,
    delivered INTEGER
);

-- 你的交易记录（手动填写）
CREATE TABLE trades (
    id INTEGER PRIMARY KEY,
    signal_id INTEGER,
    timestamp INTEGER,
    executed INTEGER,
    entry_price REAL,
    exit_price REAL,
    result REAL,
    note TEXT
);
```

## 十一、实施阶段

### 阶段一：基础连接（目标：2 天）

- [ ] 创建项目目录。
    
- [ ] OKX API 只读连接测试。
    
- [ ] CoinGecko API 连接测试。
    
- [ ] Telegram Bot 创建和消息推送测试。
    
- [ ] SQLite / DuckDB 数据库初始化。
    

### 阶段二：数据采集 + 指标（目标：3 天）

- [ ] OKX K线采集：15m / 1h / 4h。
    
- [ ] 资金费率、持仓量、标记价格采集。
    
- [ ] CoinGecko 全市场、热门币、板块数据采集。
    
- [ ] RSI / MACD / EMA / ATR / 布林带宽度计算。
    
- [ ] 数据写入数据库。
    

### 阶段三：多 Agent 协作（目标：3 天）

- [ ] OpenClaw 调度流程设计。
    
- [ ] Codex GPT-5.5 策略引擎封装。
    
- [ ] Hermes 外部信息采集封装。
    
- [ ] Claude Opus 4.7 风险审查封装。
    
- [ ] 信号生成 → 风险审查 → TG 输出链路打通。
    

### 阶段四：TG 对话与主动提醒（目标：2 天）

- [ ] `/btc`、`/eth`、`/sol` 查询命令。
    
- [ ] `/signal` 最新信号命令。
    
- [ ] `/risk BTC` 风险审查命令。
    
- [ ] `/news BTC` 外部事件命令。
    
- [ ] `/watch BTC` 重点监控命令。
    
- [ ] 止盈、止损、卖出、失效主动提醒。
    

### 阶段五：Obsidian 复盘闭环（目标：2 天）

- [ ] 每日市场日志自动写入。
    
- [ ] 信号归档。
    
- [ ] 人工交易结果记录。
    
- [ ] 复盘模板生成。
    
- [ ] 7 天稳定性测试。
    

## 十二、风险控制原则

> [!WARNING] 资金管理
> 
> - 初始资金 20U，杠杆不超过 5x。
>     
> - 单笔止损不超过本金 5%（即 1U）。
>     
> - 同时持仓不超过 2 个标的。
>     
> - 优先保守模式：宁可少交易，也不追高追空。
>     

> [!WARNING] 系统安全
> 
> - OKX API 第一版只开只读权限，不开交易权限。
>     
> - API Key 不进入日志，不进入 LLM prompt。
>     
> - 密钥使用本地 `.env` 或系统安全存储，不提交 git。
>     
> - Telegram 消息不展示任何密钥、token、账户敏感信息。
>     

> [!WARNING] 信号安全
> 
> - 每条交易信号必须包含止损和失效条件。
>     
> - 盈亏比不足时，只发市场观察，不发交易建议。
>     
> - X / 新闻 / 公告只能作为事件因子，不能单独触发交易。
>     
> - Claude 风险审查输出“观望”时，不推送为可执行交易信号。
>     

## 十三、技术栈

|用途|工具|
|---|---|
|语言|Python 3.11+|
|OKX 接入|ccxt / OKX SDK / 原生 REST + WebSocket|
|CoinGecko 接入|CoinGecko API / opencli coingecko 辅助|
|X 接入|Hermes + xurl / xitter / opencli twitter|
|技术指标|pandas-ta / ta / 自定义指标|
|数据库|SQLite，数据量增加后可切 DuckDB|
|策略工程|Codex GPT-5.5|
|风险审查|Claude Opus 4.7|
|外部信息|Hermes|
|TG 推送|OpenClaw channel / python-telegram-bot|
|调度|OpenClaw cron / APScheduler|
|复盘|Obsidian Markdown|

## 十四、测试与验收

- API 连接测试：OKX、CoinGecko、Telegram。
    
- 指标计算测试：RSI、MACD、EMA、ATR、布林带。
    
- 信号生成测试：做多、做空、观望、失效、减仓。
    
- 外部事件测试：X、公告、新闻只影响权重，不直接触发交易。
    
- Agent 协作测试：OpenClaw 能顺序调用 Codex、Hermes、Claude 并汇总。
    
- TG 推送测试：卡片格式稳定，不刷屏。
    
- Obsidian 写入测试：每日日志、信号、复盘记录完整。
    
- 稳定性验收：连续运行 7 天，无任务崩溃，无重复刷屏，无敏感信息泄漏。
    

## 十五、关联笔记

- [[01 - 币圈入门]]
    
- [[03 - 合约交易]]
    
- [[01 - 量化交易入门]]
    
- [[02 - 常见策略]]