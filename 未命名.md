```markdown
# 🤖 OpenClaw 加密货币永续合约智能交易室架构全量说明书 (Perp-Trading-Desk)

## 1. 系统总体设计概述 (System Architecture)
本系统是基于 **OpenClaw** 多智能体网关框架构建的、针对加密货币市场（以 OKX 交易所为例）的**事件驱动型（Event-Driven）永续合约量化交易系统**。
系统核心原则为：**“轻量级代码监控（24/7低功耗） + 异步事件唤醒 + 多智能体博弈决策 + 本地 Obsidian 知识库约束 + 手机端人工确认机制”**。旨在解决传统大模型轮询交易带来的高能耗、高Token成本以及情绪过拟合问题。

---

## 2. 外部看门狗监控模块 (External Watchdog & Trigger)
由轻量级纯 Python 脚本运行（基于 `CCXT` 库），常驻后台（CPU占用接近 0%），不调用任何大模型。只有满足以下任意**触发点（Triggers）**时，才通过 Webhook 异步唤醒 OpenClaw Master Agent：

* **时间触发器 (Time-Based)**：每到 1H / 4H K线确认收线整点。
* **异常指标触发器 (Data-Anomaly)**：
    * **资金费率 (Funding Rate)**：绝对值超过自定义风险阈值（如年化 > 50%）。
    * **持仓量 (Open Interest, OI)**：15分钟内出现异常筹码脉冲（如瞬间激增 > 5%）。
    * **剧烈波动 (Volatility)**：价格瞬间穿透 1H 布林带上轨/下轨。

---

## 3. OpenClaw 虚拟交易室团队配置 (Virtual Trading Desk & LLM Selection)

```

┌──────────────────────────────┐ │ 首席投资官 (Master Agent) │ ──> 发送决策至 飞书/Telegram └──────────────────────────────┘ │ ┌───────────────────────────┼───────────────────────────┐ ▼ ▼ ▼ ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐ │ 技术分析总监 │ │ 筹码流动性专家 │ │ 舆情情报官 │ │ (Technical Sub) │ │ (Microstructure)│ │ (Sentiment Sub) │ └─────────────────┘ └─────────────────┘ └─────────────────┘ │ │ │ └───────────────────────────┼───────────────────────────┘ ▼ ┌──────────────────────────────┐ │ 首席风险官 (CRO / Risk Agent)│ ──> [一票否决权] 读取 Obsidian └──────────────────────────────┘

````

### 👥 核心主控路由
#### 🟢 首席投资官 (CIO / Master Agent)
* **推荐 LLM**：`Claude 4 Sonnet` (Standard Mode)
* **岗位职责**：整个系统的指挥官。接收外部看门狗的异动报告，依次调度各子代理开展研究，主持多空辩论，最终汇总各方意见并拍板生成“交易提案（Ticket）”。

### 📊 专家子代理团 (Sub-Agents)
#### 🔵 技术分析总监 (Technical Director)
* **推荐 LLM**：`OpenAI o3-mini` (Medium Thinking)
* **数据信息元**：1H/4H 的 EMA趋势、布林带偏离度、RSI 动量指标、**ATR（真实波幅）**。
* **核心任务**：调用 Python 指标工具，将冰冷的K线价格转化为“多空技术面相对强弱状态”。**输出 ATR 给风控官用于计算止损空间**。

#### 🔴 筹码流动性专家 (Derivatives Quant Expert)
* **推荐 LLM**：`OpenAI o3` 或 `DeepSeek-R1` (Extended Thinking Mode)
* **数据信息元**：OKX 实时资金费率、全网持仓量（OI）变动、全网爆仓单数据（Liquidation）、主动买卖比（Taker Buy/Sell Volume）、订单簿挂单比（Order Book Imbalance）。
* **核心任务**：**全系统防过拟合核心。** 开启深度思考链，专门识破盘面的无量空头踩踏（Short Squeeze）、主力诱多、多头拥挤等衍生品博弈骗局。

#### 🟡 全球舆情情报官 (Sentiment Officer)
* **推荐 LLM**：`DeepSeek-V3.1` 或 `Gemini 2.0 Flash` (Fast Mode)
* **数据信息元**：Twitter/X 关键KOL言论、大额链上异动（Whale Alert）、美股宏观大盘（纳斯达克/标普500）盘前盘后动量。
* **核心任务**：利用超长上下文和低成本吞吐，快速提炼当前市场的宏观情绪，给出利好/利空的情绪评分。

### 🛡️ 风控与合规层
#### 🔒 首席风险官 (CRO / Risk Agent)
* **推荐 LLM**：`Claude 4 Sonnet` (Strict Structured Output Mode)
* **核心权限**：**拥有一票否决权（Guardrail）**。
* **数据信息元**：OKX 账户可用保证金、持仓维持保证金率、单笔风险暴露（VaR）、**Obsidian风控铁律文件**。
* **运行机制**：严厉的合规官。大模型只负责提取参数，严格根据技术总监给出的 ATR 用硬编码数学公式强算强平价与仓位。若开仓超出 2% 本金风险或违反 Obsidian 里的车位控制红线，直接拦截并降杠杆，拒绝不合格的交易提案。

---

## 4. 本地 Obsidian 知识库与数据库集成 (Obsidian Integration)

本系统不引入重型数据库，直接依赖 Mac 本地 Obsidian 文件夹（Markdown 文件群）作为 Agent 的“长期记忆”与“结构化账本”。

### 💾 4.1 知识库（Knowledge Base - 输入）
* **存储路径**：`~/Obsidian/TradingVault/Core_Rules/`
* **核心文件**：
  * `SOP_Risk_Control.md`（手写风控红线、最大回撤容忍度、单日最大亏损强制拔线死线）
  * `SOP_Strategy_Bias.md`（用户偏好的交易风格：如左侧逆势、右侧顺势、严禁扛单原则）
* **运行机制**：OpenClaw CRO 启动时，使用 `LocalFileReadTool` 预加载这些 Markdown 文件，作为 System Prompt 的“硬性动态约束”，实现带有用户强烈防御风格的交易逻辑。

### 📝 4.2 数据库（Database Ledger - 输出）
* **存储路径**：`~/Obsidian/TradingVault/Trade_Logs/{{YEAR}}/{{MONTH}}/`
* **生成机制**：每当一笔交易执行完毕（或被拒绝），OpenClaw CIO 必须调用 `LocalFileWriteTool` 自动生成一篇结构化的笔记，文件名格式为 `YYYY-MM-DD_SYMBOL_DIRECTION.md`。
* **文件模板 (Obsidian YAML Frontmatter)**：

```markdown
---
date: {{DATE}}
asset: {{SYMBOL}}
direction: {{DIRECTION}}
leverage: {{LEVERAGE}}
pnl_pct: 0.0
status: "OPENED"
tags: [TradingLog, OpenClawPerp]
---

# 📈 交易执行报告

## 1. 团队决策摘要
* **技术分析总监**: {{TECH_REPORT}}
* **筹码流动性专家**: {{QUANT_REPORT}}
* **首席投资官结辩**: {{CIO_DECISION}}

## 2. 风险要素记录
* **入场时 ATR 波动率**: {{ATR_VALUE}}
* **强平价格安全边际**: {{LIQ_MARGIN}}

## 3. 实盘跟踪
* [ ] 止损已挂单 (OKX)
* [ ] 止盈已挂单 (OKX)
````

## 5. 核心交易防过拟合设计 (Anti-Overfitting Design)

- **绝对价格脱敏**：禁止向分析师 Agent 喂长周期的绝对价格（如：68541.2 美金），一律由 Python 工具清洗为**相对指标描述**（如：“当前价格偏离 20 EMA 为 +1.2%，处于超买区”），防止大模型对历史绝对价格产生“记忆记忆偏见”。
    
- **制度对抗机制**：技术分析总监（看K线追随趋势，极易产生近因效应追涨杀跌）与筹码专家（看费率与爆仓，逆向博弈思维）形成方向性制衡，必须通过两者的对齐辩论才能通过提案。
    
- **硬编码公式兜底**：风控 Agent 内部固化数学公式，大模型不负责计算具体仓位和强平价，防止大模型在极端行情下产生数字幻觉。
    

## 6. 数据流与事件生命周期 (Data Flow & Lifecycle)

```
[ Watchdog 触发 ] ──> [ 唤醒 CIO (Master Agent) ] 
                        │
                        ├──> [ 调度 技术/筹码/舆情 Agent 分头提取 OKX 数据并产出报告 ]
                        │
                        └──> [ CIO 主持多空博弈辩论 ] ──> [ 形成开/平仓草案 ]
                                                            │
[ 手机端确认下单 ] <── [ 发送 Ticket 到 飞书/TG ] <── [ CRO 进行 Obsidian 规约审计(通过/修正) ]
```

## 7. 核心数据交换 Schema 示例 (Structured Output)

当团队讨论通过并经由 CRO 审计后，由 **CRO Agent** 最终输出给 OpenClaw 执行工具的结构化 JSON。GPT-5.5 需严格按照此 JSON 格式输出交易信号：

```json
{
  "status": "APPROVED",
  "trade_details": {
    "symbol": "BTC-USDT-SWAP",
    "direction": "OPEN_SHORT",
    "margin_mode": "ISOLATED",
    "leverage": 3,
    "entry_strategy": "MARKET",
    "stop_loss_price": 71200.0,
    "take_profit_price": 64500.0
  },
  "risk_assessment": {
    "account_margin_usage_pct": 15.5,
    "est_liquidation_price": 92500.0,
    "max_loss_usd": 200.0,
    "rationale": "EMA4H 触顶回落，同时资金费率极高 (>100%年化)，多头持仓开始松动，存在 Long Squeeze 风险。结合 Obsidian 核心风控规则，将杠杆严格限制在 3x，满足单笔 2% 风险敞口约束。"
  }
}
```

请根据此架构说明书，为我编写：

1. **外部 Watchdog 的 Python 核心监控脚本（基于 CCXT，含异常阈值检测）。**
    
2. **OpenClaw 的主、子智能体（CIO、技术、筹码、风控）的 System Prompt 与调用流代码。**
    
3. **读写 Obsidian 本地 Markdown 文件的 Tool 工具链。**